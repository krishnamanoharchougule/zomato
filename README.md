"# zomato" 
- I used 4 main libraries, numpy & pandas for standard python-csv tasks, matplotlib for functioning of different fiugres and plots, seaborn for the added inbuilt functionality and utility of different prebuilt graph templates
- first I loaded the sample csv and then i cleaned the rating column fields to get rid of the denominator '5' indicating max rating from each field for easier processing.
- then I converted the votes column fields to numeric values instead of being processed as strings to prevent any further complications during processing.
- after that for each objective/graph I used different functions to extract relevant data out of the loaded dataframe.
- some of these functions include sum(), split(), groupby(), max(), countplot(), heatmap()
  
